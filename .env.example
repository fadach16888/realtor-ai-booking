# 客戶端的預約流程不需要任何金鑰。
# 預約資料只會寫到 data/appointments.json。
# 確認信只會產生本機 HTML 預覽，不會真的寄出。

# 後台 /admin/appointments 的登入帳密（必填）。
# 沒有設定 ADMIN_PASSWORD 時，後台與相關 API 會直接回 503，不會裸奔。
# 使用方式：複製這個檔案成 .env.local，把密碼換成自己的。
ADMIN_USER=admin
ADMIN_PASSWORD=請換成你自己的長密碼
